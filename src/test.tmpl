<!DOCTYPE html>
<html>
<head>
<meta http-equiv="Content-type" content="text/html; charset=utf-8"/>
<title><%= htmlWebpackPlugin.options.title %></title>
<style>
	.panel {
		display: inline-block;
		position: relative;
		height: calc(100vh - 20px);
		width: calc(50% - 20px);
		overflow: hidden;
	}

	.panel .content {
		overflow: auto;
		height: calc(100vh - 160px);
		padding: 8px;
	}

	.panel #quick-compose,
	.panel #full-compose {
		background: white;
		position: absolute;
		bottom: 0;
		width: 100%;
	}
</style>
</head>
<body>
	<div class="panel">
		<div class="content">
		</div>
		<div id="quick-compose">
		</div>
	</div>

	<div class="panel">
		<div id="full-compose">
		</div>
	</div>
</body>
</html>
